[package]
name = "boid-simulation"
version = "0.1.0"
edition = "2024"

[dependencies]
neural-network = { path = "../neural-network"}
nalgebra = { version = "0.34.1", features = ["rand-no-std"] }
rand = "0.9.2"
test-case = "3.3.1"

