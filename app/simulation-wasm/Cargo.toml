[package]
name = "lib-simulation-wasm"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.9.2"
wasm-bindgen = "0.2.100"
boid-simulation = { path = "../../boid/simulation"}
getrandom = { version = "0.3.4", features = ["wasm_js"] }

[lib]
crate-type = ["cdylib"]
